#include <bits/stdc++.h>
#define all(v) (v).begin(), (v).end()
#define ll long long
using namespace std;
/*
    
*/
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll t, n; char c;
    cin>>t;
    
    while (t--) {
        cin>>n>>c;
        while (n--) {
            cout << c;
        }
        cout << '\n';

    }

    return 0;
}
