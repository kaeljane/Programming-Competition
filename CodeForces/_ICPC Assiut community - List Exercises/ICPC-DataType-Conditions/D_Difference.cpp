#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    long long a,b,c,d;
    long long x;
    cin>>a>>b>>c>>d;
    x = (a*b) - (c*d);
    cout<<"Difference = "<<x<<endl;

    return 0;
}